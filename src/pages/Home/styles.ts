import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',//'#f5f5fa',
    width: '100%',
  },
  b: {
    position: 'absolute',
    right: 14,
    top: 14,
    zIndex: 5,
    flexDirection: 'row',
  },
});

export default styles